# Umax_LangChainRagProject — 企业级 RAG 知识库
## 产品需求与开发方案（第一版）

> 定位一句话：**把公司散落的文档，变成一个"随问随答、句句有出处"的企业智囊；一套系统做成模板，卖给一家复制一家。**
> 目标客户：中小型企业（10~500 人）——有产品手册、规章制度、合同、培训资料要查，又养不起 IT 团队做 AI 的公司。
> 经营模式：个人 OPC + vibecoding 开发；主打**私有化交付**（数据不出客户门）+ 授权费/年维保，后期扩多租户 SaaS。

---

## 一、产品是什么（打个比方）

把产品想象成公司新来的一个"什么都知道、还从不说累"的资深老员工：

1. **一个图书管理员（入库）**：把公司的 Word、PDF、扫描件、Excel 倒进来，他自动"翻译"成机器能读懂的卡片，分门别类放进卡片柜。
2. **一个检索能手（检索）**：员工提问时，他几秒钟内从几千份文档里翻出最相关的几段卡片——既按关键词找，也按"意思相近"找。
3. **一个会标注出处的答主（生成）**：大模型把这几段卡片组织成通顺的回答，并在下面注明"这句话出自《员工手册》第 3 章"——可查证，不瞎编。

**为什么企业会买它**：新员工不用追着老员工问；客服不用翻几百页手册；老板不用担心"骨干一走，知识全带走"。

**为什么买你的而不是自己拿开源的凑**：开箱即用（一键部署 + 初始化向导）、数据不出门（私有化）、有人管售后（年维保）、后台能自己换模型看用量——中小公司要的是"省事"，不是"框架"。

---

## 二、功能需求规划

### 2.1 第一期（MVP，最小可用版本）—— 只做"离不开"的功能

MVP 的原则：**功能少而完整**。客户能完整走通"传文档 → 建索引 → 提问 → 带引用的回答"这条路径。

#### A. 知识库与文档管理（核心，占工作量大头）

| 功能 | 通俗解释 | 优先级 |
|------|---------|--------|
| 知识库管理 | 按部门/主题建多个库（如"产品手册库""制度库"），文档各归各库 | 必须 |
| 文档上传 | 拖拽上传 PDF / Word / Excel / PPT / TXT / Markdown，支持批量 | 必须 |
| 解析流水线 | 后台自动"解析→切块→向量化"，界面显示进度和失败原因，失败可重试 | 必须 |
| 扫描件 OCR | 图片型 PDF（扫描的合同、盖章文件）也能读出文字 | 必须 |
| 图表入库 | 文档里的图表、照片，用视觉模型转成文字描述一起进索引，能被搜到 | 必须 |
| 重建索引 | 换了向量化模型或调了切分参数，一键对全库重跑 | 必须 |
| 分块预览 | 管理员能看到"文档被切成了哪些小块"——排查"为什么没答对"的第一工具 | 必须 |

#### B. 智能问答（产品的卖点）

| 功能 | 通俗解释 | 优先级 |
|------|---------|--------|
| 聊天界面 | 对话式提问，支持多轮追问 | 必须 |
| 引用溯源 | 每个回答下方列出出处（文档名+段落），点击看原文——企业客户信任的来源 | 必须 |
| 混合检索+重排 | 关键词和语义两路同时找，再用重排模型精选——企业级效果的及格线 | 必须 |
| 权限内检索 | 每个人只能问出"他被授权的知识库"里的内容 | 必须（简版：按知识库授权） |
| 未命中兜底 | 知识库里没有的，明确回答"资料里没有"，禁止瞎编 | 必须 |
| 会话历史 | 历史对话保留、可回看 | 必须 |
| 传图提问 | 用户上传图片提问（一期消息数据结构就按多模态设计，界面二期放开） | 数据结构必须 / 界面第二期 |
| 语音输入 | 说话提问（ASR 转写） | 第三期 |

#### C. 模型接入与用量（AI 要花钱，得有闸门）

| 功能 | 通俗解释 | 优先级 |
|------|---------|--------|
| 后台换模型 | 管理员在后台随时更换大模型厂商/型号，改完即生效，不用改代码重启 | 必须 |
| 客户自带 key | 默认客户填自己的 API key（BYO-key）；密钥加密存储、界面打码、不回传明文 | 必须 |
| 分场景配模型 | 聊天/向量化/重排/识图四类场景各配各的模型——日常问答用便宜的，识图用视觉的 | 必须 |
| 备用自动切换 | 主模型限流或报错，自动切到备用模型，用户无感 | 必须 |
| 用量台账 | 每次调用记一笔：谁、哪个库、哪个模型、多少 token、几点钟——成本与计费的事实来源 | 必须 |
| 用量看板 | 管理员看用量统计和成本折算（按人/按库/按天） | 必须（简版） |
| 配额与告警 | 每用户设每日/每月上限，超了提醒或暂停 | 必须（简版） |

#### D. 部署与授权（"可复制"的载体）

| 功能 | 通俗解释 | 优先级 |
|------|---------|--------|
| 一键部署 | 新客户 = clone 模板仓库 + 填一份配置 + 跑一条 Docker Compose 命令，半天交付 | 必须 |
| 配置驱动 | logo、品牌名、提示词、检索参数全在配置里，**不动代码**——可复制的生命线 | 必须 |
| License 授权 | 授权文件控制到期时间、绑定客户机器——年费续费的商业抓手 | 必须 |
| 初始化向导 | 部署完打开网页，引导管理员建账号、填 key、传第一批文档 | 必须（简版） |

### 2.2 第二期（跑通 MVP 后的企业级补齐与增长）

- **用户与权限体系**：角色/部门权限，可见性过滤做在**检索层**而不是展示层（企业级硬指标）。
- **开放 API**：提供 OpenAI 兼容接口 + API key 管理（作用域+配额），客户把知识库嵌进自己的钉钉/企微/内部系统。
- **白标设置**：后台上传 logo、改品牌名、绑客户域名——"像给客户定做的一样"。
- **文档连接器**：飞书/钉钉/企业微信/网盘自动同步，文档更新自动重入库。
- **评测体系正式化**：金标准问答集 + Ragas 自动评分，每次调参数跑一遍，用数据证明"更准"。
- **传图提问界面**放开。
- **多租户 SaaS 版**：一套系统服务多客户，订阅制收费（一期做架构预留，二期后上产品）。

### 2.3 明确不做的事（第一期克制清单）

- ❌ 微服务、K8s：单机 Docker Compose 足够撑起中小客户，别把运维做成自己的第二主业
- ❌ 自研解析/OCR/嵌入模型：全用开源成熟件，自研最不值当
- ❌ 多模态向量检索（ColPali 那类）：太新太重，用"图片转文字描述"的务实方案
- ❌ 代码级定制：客户的一切差异收敛到配置文件——定制一旦下沉到代码，复制性就死了
- ❌ Agent 自主行动、复杂工作流编排：等有真实客户场景再说（LangGraph 架构预留）

---

## 三、技术方案（用大白话讲）

### 3.1 整体架构图景

```
用户浏览器（聊天界面 / 管理后台）
   │
   ▼
后端服务（FastAPI，"管家"）
   ├─ 入库流水线：收文档 → MinerU 解析 → 切块 → 向量化 → 存入 PostgreSQL
   ├─ 问答链路：问题 → 混合检索（关键词+向量）→ 重排精选 → 组装提示词 → 调大模型
   ├─ 模型网关：所有 AI 调用走这一扇门（统一记台账 / 后台换厂商 / 故障自动切换）
   │        └──▶ 大模型 API（客户自己的 key：DeepSeek / 通义 / 智谱 / 本地 Ollama）
   ├─ 异步任务队列：解析文档这类慢活排队慢慢干，不卡住界面
   └─ PostgreSQL + pgvector：一本账管所有——用户/文档/切块/向量/用量/授权
```

三个角色，用人来比喻：
- **后端服务**：餐厅的"前厅+管家"，接单、调度、记账全靠它。
- **MinerU 解析服务**：把扫描件"翻译"成能读的字的"录入员"——RAG 质量的第一道工序。
- **模型网关**：公司唯一的"采购窗口"，谁家模型好用就买谁家，每一笔开销记在同一本台账上。

### 3.2 各部分选型（附选择理由，都是成熟方案，不冒险）

**① 编排框架：LangChain + LangGraph**

| 组件 | 用途 | 结论 |
|------|------|------|
| LangChain | 检索链、切块、向量库对接等现成组件 | ✅ 一期核心 |
| LangGraph | 带状态的智能体编排（多步检索、工具调用） | ✅ 三期 Agentic RAG 用 |

> 为什么不全押 LangGraph：一期需求是"检索问答"这条直线，LangChain 的现成组件最快；Agent 化等真实场景出现再上，避免过度设计。

**② 后端与数据库**

- 语言框架：**Python + FastAPI**（AI 生态全在 Python，LangChain 原生）。
- 异步任务：**ARQ**（轻量 Redis 队列，解析文档用；嫌小可换 Celery）。
- 数据库：**PostgreSQL + pgvector**——一个库同时当"账本"和"卡片柜"，客户机器上少装一个组件，交付和运维成本大幅下降；百万级向量内完全够用（中小客户远到不了这个量级）。
- 向量库抽象成接口：将来真到千万级，换 Qdrant/Milvus，业务代码不用重写。

**③ 文档解析（决定 RAG 上限的第一道工序，也是第一坑）**

| 选项 | 说明 | 结论 |
|------|------|------|
| **MinerU（推荐）** | 开源，中文 PDF/扫描件/表格还原强，本地部署数据不出门 | ✅ 主力 |
| python-docx / openpyxl / python-pptx | Office 文件直接读结构 | ✅ 配合用 |
| Docling | 同类竞品 | 备选 |

> 为什么说它是第一坑：扫描件、盖章合同、图表截图在企业文档里到处都是，解析读错了，后面检索和生成全白搭。**阶段 0 必须用客户提供的真实脏文档验证，不能用干净的样例文档自嗨。**

**④ 检索与生成**

- 向量化（Embedding）：**BGE-M3** 自托管（中英文、长文本表现好；也留 API 接法）。
- 检索：**混合检索**——BM25 关键词 + 向量语义两路并行，RRF 融合，再过一道 **bge-reranker-v2-m3** 重排精选。纯向量检索在企业文档上效果不稳，这是及格线配置。
- 生成：大模型走客户 key（DeepSeek / 通义 / 智谱等），统一 OpenAI 兼容协议。

**⑤ 模型网关（自建薄层，握在自己手里）**

- 统一走 **OpenAI 兼容协议**，加三样自建能力：**模型能力注册表**（每个模型登记是否支持视觉/函数调用/JSON 输出，按场景路由）、**fallback 链**（主模型挂了自动切备用）、**用量记账**（每次调用取响应里的 usage 字段落台账）。
- OneAPI/NewAPI 这类现成网关是备选，但它们是独立系统；用量台账和后台切换是本产品的核心卖点，握在自己手里更稳。

**⑥ 前端**

- **Next.js + shadcn/ui**：聊天界面（含引用溯源）和管理后台放同一个前端项目。
- 关键决策：**消息存储从第一天就用结构化 content parts（`[{type:text},{type:image_url}]`），不存纯字符串**——以后放开传图提问不用动数据模型。

**⑦ 评测与可观测**

- 评测：**Ragas / DeepEval** + 金标准问答集——向客户证明"更准"的唯一方式。
- 可观测：**Langfuse 自托管**——私有化客户环境里不能依赖 LangSmith 云服务。

**⑧ 部署**

- Docker Compose + .env + 部署脚本，一台 4核8G 起步；全站 HTTPS；Sentry 收报错；内置健康检查面板（客户能自助看各组件状态，减少找你售后）。

### 3.3 数据怎么存（核心数据表，大白话版）

- `users`：用户（一期单客户，表结构预留 `tenant_id`，二期 SaaS 直接启用）
- `knowledge_bases / documents / chunks`：知识库、文档、切块（切块里存向量+原文+元数据+授权范围）
- `model_configs`：模型配置（厂商/地址/加密 key/模型名/能力标签/是否默认/是否备用）——**后台换模型就是改这张表**
- `usage_records`：用量台账（谁/哪个库/哪个模型/多少 token/何时）——成本与计费的事实来源，也是续费谈判时的数据弹药
- `conversations / messages`：会话与消息（messages 存结构化 content parts，天生多模态）
- `licenses`：授权（到期时间、机器指纹、签发记录）
- 安全底线：客户 key 落库加密（主密钥放环境变量）、界面打码、保存后不回传明文、操作留审计日志

---

## 四、开发计划（分四步走）

### 阶段 0：技术验证（第 1~2 周）
- 找 1~2 家目标客户要 **20 份真实脏文档**（含扫描件/表格/图表的 PDF、Word）
- 跑通最简链路：上传 → MinerU 解析 → 切块 → BGE-M3 向量化 → pgvector → 混合检索 → 重排 → 大模型生成带引用回答
- 对比不同切分参数/检索组合，把"能答对"的基线调出来
- **里程碑：一条命令起全套链路，20 份文档的问题能答对八成，引用可点开验证**

### 阶段 1：MVP（第 3~8 周）—— 先做交付闭环，不做企业级花活

> **开发模式（2026-09-19 确立）：全程 TDD**——每个功能先写失败测试再实现（红→绿→重构）；测试框架 pytest，外部依赖中数据库用真 PG 容器（独立测试库 `umaxrag_test`，函数级 TRUNCATE 隔离），LLM/解析等远程服务用 MockTransport 契约测试、真调用只留冒烟。代码在 `backend/`，一键回归：`cd backend && pytest`。
- 文档管理：知识库/上传/异步解析流水线/进度/失败重试/重建索引/分块预览
- 问答：聊天界面 + 引用溯源 + 混合检索重排 + 按库授权 + 未命中兜底 + 会话历史
- 模型网关：分场景配置（后台可改）/BYO-key 加密存储/fallback/用量台账与看板
- 部署：Docker Compose 一键脚本 + 初始化向导 + License 校验
- 简版评测：10~20 条金标准问答，每次改参数跑一遍防退化
- **里程碑：找 1~2 家种子客户试用，走通"部署→传文档→问答→后台换模型→看用量"完整闭环**

### 阶段 2：企业级补齐（第 9~14 周）
- RBAC 与部门级权限过滤（过滤在检索层）、审计日志
- 开放 API（OpenAI 兼容端点 + key 管理 + 配额）
- 白标设置（logo/品牌/域名）
- 评测体系正式化（Ragas + 金标准集管理界面）
- 传图提问界面放开
- **里程碑：第一家付费私有化客户交付**

### 阶段 3：规模化复制（上线后按客户反馈定优先级）
- 文档连接器（飞书/钉钉/企微/网盘）
- **行业模板包**（预置提示词+语料结构+切分参数，按行业卖）——真正卖钱的"复制品"
- LangGraph Agentic RAG（多步检索/工具调用）
- 多租户 SaaS 版、语音输入

### 团队与预算参考（个人 OPC 配置）

| 角色 | 人数 | 说明 |
|------|------|------|
| 开发（你 + AI 编程） | 1 | vibecoding 主力；阶段 0 的链路必须本人吃透，否则售后接不住 |
| 测试/验收 | 0.5（可兼任） | 阶段 1 起用金标准集跑回归 |

- 人力外的成本：开发期一台 4核8G 云主机 + 自己的模型 API 测试费（每月几百元级）；交付期服务器由客户出
- 每户交付成本目标：脚本成熟后半天~1 天——这是"可复制"是否成立的硬指标

### 主要风险与对策

| 风险 | 大白话 | 对策 |
|------|--------|------|
| 解析质量不过关 | 扫描件读成乱码，答案必错 | 阶段 0 用真实脏文档验证；分块预览让客户自助排查；解析服务独立成模块可替换 |
| 证明不了"更准" | 客户问你凭什么叫企业级 | 金标准评测集第一天就建，用数据说话 |
| 定制失控 | 每个客户都要求改代码 | 铁律：一切差异进配置；定制需求进模板包或二期池子 |
| 售后拖死个人 | 客户机器出点事都找你 | 部署文档+自检脚本+健康面板写进交付物；年维保明确响应范围 |
| 换 embedding 模型翻车 | 向量全废，全库要重跑 | 重建索引做成后台一键任务+进度显示；建库时提醒客户锁型 |
| 模型厂商涨价/停服 | 上游一变全线瘫痪 | 网关抽象 + OpenAI 兼容协议，随时换 |
| 密钥泄露 | 客户 key 出事就是事故 | 加密存储/界面打码/不回传明文/审计日志 |

---

## 五、下一步建议（按顺序）

1. ✅ **定项目名与独立仓库**：已定名 Umax_LangChainRagProject，GitHub 仓库 https://github.com/Levin-Sun/Umax_LangChainRagProject 已建立
2. ✅ **收 20 份真实脏文档**：已到位（电商行业，`DirtyDocs/`，含错别字/重复段落/版本冲突/术语变体/私人噪声/中英混杂/作废内容等全部考察点）
3. ✅ **阶段 0 技术验证**：全链路跑通，金标准 20 题两种配置均 20/20（详见第六章执行记录）
4. **确定第一个种子行业**（电商已具备语料），细化阶段 1 任务清单，进入开发

---

## 六、阶段 0 执行记录与交接说明（2026-09-19，交接给开发 Agent）

> 本章为阶段 0 的实测记录和环境现状，接手开发的 Agent 请先通读本章再动工，可避免重复踩坑。

### 6.1 验证结论：阶段 0 通过

| 配置 | 金标准 20 题 | recall@5 | MRR |
|------|------------|----------|-----|
| 纯 BM25 基线 | 20/20（100%） | 20/20 | 1.000 |
| BM25+向量 RRF+重排（本地 bge 模型） | 20/20（100%） | 20/20 | 0.975 |
| BM25+向量 RRF+重排（百炼 API，2026-09-19 切换后复核） | 20/20（100%） | 20/20 | 0.975 |

- 评测报告：`archive/stage0/eval/report_20260919_1819.md`（BM25 基线）、`archive/stage0/eval/report_20260919_2126.md`（本地混合检索）、`archive/stage0/eval/report_20260919_2144.md`（百炼 API 混合检索）
- **关键发现**：20 篇短文档的小语料上 BM25 已经满分，向量未体现出增益（MRR 略降）。这不否定混合检索——BM25 强在专名/SKU 编号精确匹配，向量强在同义改写（如"结算账期/回款账期"），增益要在**更大语料**上才能显现。生产配置仍按 §3.2 用混合检索，但阶段 1 应在文档量上百后重跑 `archive/stage0/compare_retrieval.py` 复核。
- 含金量佐证：版本冲突题（FBA 时效、618 截止日、项链重量）全部正确指出冲突而非擅自裁决；作废删除线内容、"优选可用最优不行"边界题全对。

### 6.2 仓库现状（交付物清单）

> **归档说明（阶段 1）**：本节目录已整体移至 `archive/stage0/`——检索内核已服务化进 `backend/`（对应任务 1~4 全部 TDD 通过），stage0 脚本仅作历史留档。注意：stage0 的 `ingest.py` 开头 `DROP TABLE chunks`，而真库 `umaxrag` 的 `chunks` 表已是阶段 1 新结构（旧数据留档为 `chunks_stage0_backup`），**不要再对真库运行 stage0 脚本**。

```
docker-compose.yml        pgvector:pg16（localhost:5432，用户 rag / 密码 rag_dev_2026 / 库 umaxrag）
archive/stage0/
  config.py               全部配置 .env 驱动
  embed_provider.py       嵌入双 provider（百炼 API / 本地 sentence-transformers）+ 重试退避
  ingest.py               入库：读取→切块→向量化→写 pgvector（--no-embed 走纯 BM25 基线）
  retrieve.py             BM25+向量 RRF 融合+重排；向量/重排不可用时优雅降级
  generate.py             qwen3.7-flash 带引用生成；重试退避（2/5/10s）
  query.py                问答 CLI
  eval.py                 金标准评测（产出 Markdown 报告）
  compare_retrieval.py    三档纯检索对比（BM25 / +RRF / +重排，不调 LLM）
  eval/golden_qa.json     20 题金标准（含考察点与校准记录）
  .env / .env.example     密钥与环境（.env 不入库）
DirtyDocs/                20 份电商脏文档语料
```

复现命令：`docker compose up -d` → `cd archive/stage0 && python ingest.py` → `python query.py "问题"` → `python eval.py` → `python compare_retrieval.py`

### 6.3 环境与已知问题（接手前必读）

1. **百炼 API 现状（2026-09-19 更新：已全部放行）**：专属端点 `ws-vfxy0f3ujhki8ob7.cn-beijing.maas.aliyuncs.com`（`sk-ws-` 工作空间 key，配置在 `archive/stage0/.env`）此前仅放行 chat，后经控制台开通 `qwen3.7-text-embedding`（1024 维）与 `qwen3.7-text-rerank`，实测均已可用。**当前生产配置：`EMBED_PROVIDER=bailian`，chat/embedding/rerank 全走百炼 API**，已重新入库（`ingest.py`）并复核评测 20/20。
2. **本地对照方案（随时可切回）**：`EMBED_PROVIDER=local` 走本机 `BAAI/bge-small-zh-v1.5`（512 维）+ `BAAI/bge-reranker-base`（模型已缓存，HF_ENDPOINT=hf-mirror.com）。注意：**切换 embedding 提供方 = 向量维度与语义全变，必须重跑 `ingest.py` 重新入库**。
3. **chat 接口有间歇性 403 / 断连**：曾导致一轮评测 17/20，三题全败在生成调用而非检索（`report_20260919_2119.md` 留档为证）。`generate.py` / `embed_provider.py` 已内置重试退避解决。**评测中偶发 1 题失败，先看是不是这类瞬时错误，不要急着调检索参数。**
4. **Docker Hub 国内直连不通**：pgvector 镜像经 `docker.1ms.run` 拉取后重新打 tag。后续拉镜像（MinerU 等）同样需要镜像源（可依次试 docker.1ms.run / docker.m.daocloud.io / docker.1panel.live）。
5. 本机 Python 3.14.7 + torch 2.14 CPU + sentence-transformers 实测可用（依赖见 `archive/stage0/requirements.txt`）。
6. **密钥安全**：`.env` 已被 .gitignore 排除；当前 key 在沟通记录中暴露过，建议在百炼控制台轮换后更新 `archive/stage0/.env`。

### 6.4 评测集校准记录（防止误判）

- 前三轮 95%→90%→100% 中的"失败"多为**评测标准过严**：回答正确但未写出题目没问的延伸细节。已校准（延伸细节记入 note 作加分项），每题校准理由写在 `golden_qa.json` 的 note 字段。
- 生成模型有运行间非确定性，单轮 ±1 题波动属正常；连续两轮同一题失败才值得排查。
- 原则：**评测集随事实校准，但不能为分数放水**——每次校准都要留记录。

### 6.5 阶段 1 开发指引（下一步，按优先级）

1. **MinerU 解析接入**：当前语料全是 txt，阶段 1 必须支持 PDF/Word/Excel/扫描件（镜像源问题见 6.3-4）
2. **FastAPI 服务化**：stage0 代码是可运行的检索内核种子，重构为服务（上传/解析状态/检索/问答 API）+ ARQ 异步任务队列
3. **模型网关**：分场景配置（chat/embedding/rerank/视觉各一）、fallback 链、用量台账（表结构见 §3.3）
4. **前端**：Next.js 聊天界面 + 引用溯源 + 管理后台简版
5. **数据模型落地**：`knowledge_bases / documents / chunks / model_configs / usage_records` 等表（§3.3），预留 tenant_id
6. ~~百炼 embedding/rerank 生效后切回 API~~（✅ 2026-09-19 已切换并复核 20/20）；仍需在文档量上百的语料上重跑 `compare_retrieval.py` 验证向量增益
7. 架构铁律不变：pgvector 起步、混合检索+重排、BYO-key、配置驱动拒绝代码定制、Docker Compose 交付
